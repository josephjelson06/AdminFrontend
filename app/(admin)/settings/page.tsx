'use client';

/**
 * Settings Page
 * 
 * Admin page for platform configuration.
 * Uses the SettingsManager component from admin/settings.
 */

import { SettingsManager } from '@/components/admin/settings';

export default function SettingsPage() {
    return <SettingsManager />;
}
