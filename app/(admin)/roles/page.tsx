'use client';

/**
 * Roles Page
 * 
 * Admin page for managing roles and permissions.
 * Uses RolesList component from admin/roles.
 */

import { RolesList } from '@/components/admin/roles';

export default function RolesPage() {
    return <RolesList />;
}
