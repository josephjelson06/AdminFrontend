'use client';

/**
 * Hotel Settings Page
 * 
 * Hotel profile management.
 * Uses HotelSettingsManager from components/hotel/settings.
 */

import { HotelSettingsManager } from '@/components/hotel/settings';

export default function HotelSettingsPage() {
    return <HotelSettingsManager />;
}
